# Changelog

## 0.0.2

### ✨ Updates
- Build OpenCode from the configured fork/ref during image build
- Support `--base-path` with optional ingress auto-detection

## 0.0.1

### 🎉 Initial Release
- OpenCode web UI add-on scaffold
- Configurable server hostname/port and optional auth
- Home Assistant API + CLI access inside the container
