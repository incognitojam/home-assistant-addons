---
name: "Claude Code"
description: "Terminal interface for Anthropic's Claude Code CLI with persistent auth"
version: "1.5.8"
slug: "claude_terminal"
init: false

# Supported architectures
arch:
  - aarch64
  - amd64
  - armv7

# External resources
url: "https://github.com/incognitojam/home-assistant-addons"

# Web interface configuration
ingress: true
ingress_port: 7681
panel_icon: mdi:code-braces
panel_title: "Claude Code"
panel_admin: true

# Port configuration
ports:
  7681/tcp: 7681
ports_description:
  7681/tcp: "Web terminal (not required for ingress)"

# Add-on configuration options
options:
  auto_launch_claude:
    name: "Auto-launch Claude"
    description: "Auto-start Claude on terminal open (disable for session picker)"
  auto_update_claude:
    name: "Auto-update Claude Code"
    description: "Update Claude Code CLI to latest version when opening terminal"
schema:
  auto_launch_claude: bool?
  auto_update_claude: bool?

# Volume mappings for file access
map:
  - config:rw    # Access to Home Assistant configuration files

# API access for Home Assistant integration
hassio_api: true
hassio_role: manager
homeassistant_api: true
auth_api: true
host_dbus: true

# Service startup configuration
startup: services

# Web UI access URL
webui: http://[HOST]:[PORT:7681]/
